name: xianyu-ai-monitor

on:
  schedule:
    - cron: "*/11 * * * *"
  workflow_dispatch:


jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: run python
        run: python monitor.py
# force trigger refresh
